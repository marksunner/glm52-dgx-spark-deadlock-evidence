# Climb stall #2 — headroom round (max_num_seqs=3), 2026-07-17 09:01Z
Session: fable5-headroom-round-20260717-cont. Companion thin bundle (driver auto-capture):
~/climb-stall-20260717T090144Z. Ledger §17 cont has receipts; this bundle is the rich capture.

## Timeline (UTC)
- 08:50 launch (max_num_seqs 6->3, ONLY variable; enforce/stuck_s 2000/engine 1800/GMU 0.91/KV 10.95GB)
- 08:55:23-31 fleet-wide 0x51 burst DURING BOOT WARMUP (rank0=2 rank1=4 rank2=3 rank3=3; see 0x51-lines-*.txt) — pre-payload
- 08:57:09 GLM52_GATE ARMED mode=enforce 4/4; smoke 3/3 PASS; steps [1105 x4]
- 08:58:07 stage 1 fired (4164 reps ~100K, max_tokens=1)
- 08:58:14 telemetry already step_rate 0; first RAS sample 08:58:33 frozen [1151,1151,1151,1151]
- 09:01:44 pre-registered stall rule (RAS frozen 3x90s) -> STOP. Soak-30 never fired.

## Signature — DIFFERS FROM §16 IN THE KEY DIMENSION
- RAS main TP comm frozen EQUAL [1151,1151,1151,1151], spread 0 (§16 was spread 1, rank2 one short).
- py-spy (plain+native, all 4 ranks, pyspy*-*.txt): ALL FOUR main threads host-spinning in
  sched_yield(libc) inside marlin MoE custom ops — rank0/rank2/rank3 in torch.ops moe_wna16_marlin_gemm
  (_custom_ops.py:2383 via marlin_moe.py:133), rank1 in moe_align_block_size (_custom_ops.py:2090,
  marlin_moe.py:326 — same fused_marlin_moe call, one line earlier). Eager prefill frames
  (FULL_DECODE_ONLY graphs; prefill is eager).
- Memory at freeze (freem-*.txt): used ~123.4-124.1/124.6 GB, free ~1.0-1.2 GB, avail 0.55-1.17 GB,
  swap 3.5-4.6 GB — ALL FOUR nodes. Same picture as the §16 stall.
- 0x51 during the freeze window: ZERO (all of today's events are the 08:55 boot burst).
- Instruments: D3 correctly silent (triggers on spread; spread=0). Gate armed enforce 4/4,
  correctly silent (100K << 199,997 jurisdiction). D2 stuck_s 2000 > engine 1800 by design.

## Findings
1. **max_num_seqs 6->3 FALSIFIED as remedy** (n=2 freeze, same class, same onset ~seconds into
   100K cold prefill). Resting footprint unchanged — GMU 0.91 reserves ~113 GB; the scheduler cap
   does not touch the reservation. Headroom receipt pre-stage-1 showed the fleet at the ceiling
   AT REST (ledger §17 cont).
2. **rank2-hardware theory takes a direct hit:** the §16 signature (lone rank2 sched_yield spin in
   marlin_gemm) reproduced on ALL FOUR ranks simultaneously. §16 rank2-one-short was the leading
   edge of the same fleet-wide condition, not a node defect. "rank2 3-for-3" now reads
   "rank2 hits it first," not "rank2 is broken."
3. **Mechanism read (hypothesis, sharpened):** marlin MoE ops allocate per-call workspace
   (moe_align_block_size buffers, marlin gemm workspace). Under exhausted UMA (+swap), the
   allocation path spins (sched_yield loop) instead of failing hard — 0x51-leak-adjacent
   behavior, now caught at the exact op on 4/4 ranks. Boot itself already trips 0x51 at
   GMU 0.91 (08:55 burst, fleet-synchronized, pre-payload).
4. **The pre-registered falsification did NOT trigger** (freeze at ~1 GB free, not multiple
   GB). Memory exhaustion as sole cause stands UNfalsified — and is now the best-supported read.
5. Scoring: platform ledger (doctrine 4). Gate untested AND unimplicated; verdict still unreached.

## Obvious next variable (for Mark to pick — not executed)
GMU 0.91 -> 0.86 (pre-authorized once on 07-16 as Round B; KV bytes stay pinned at 10.95 GB so
context capacity unchanged; directly creates the UMA headroom the seqs cut could not).
Branch (c) as rank2-swap is NOT supported by tonight's evidence.
