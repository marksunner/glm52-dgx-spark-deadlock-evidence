# Stall #3 — 2026-07-17 10:03:52Z, stage-1 (60K) of the 120K-ceiling climb
## THE ROUND THAT FALSIFIED MEMORY AND LOCALIZED THE DEVICE-SIDE SPIN

Context: served ceiling lowered to 120K (ledger §19), KV 6.6 GB (floor receipt:
120,575 tok), headroom receipt PASSED — avail 3.6–4.9 GB on all four nodes at
rest (first multiple-GB round of the campaign). Full §6 receipts 4/4, gate
armed enforce 4/4. Climb stage 1 = 2500 reps ≈ 60K-token cold prefill.

Timeline: payload 10:03:40Z → D3 would_enforce (RAS frozen MISMATCH x3 polls)
10:03:52Z → RAS [1205,1204,1205,1205] (main TP comm; rank 1 one short)
frozen across 3×90 s driver samples → stall rule fired 10:07:11Z.

## Evidence in this bundle
- pyspy[-native]-{rank0,rank1,rank2,rank3}.txt — captured with docker exec -u root
  --privileged (plain exec now gets ptrace Permission denied).
  ALL FOUR MainThreads: sched_yield ← 10 libcuda frames ← cuLaunchKernel ←
  cudaLaunchKernel ← marlin::Marlin sm80_kernel_bfloat16_u8b128 device stub ←
  marlin_mm (marlin.cu:535) ← marlin_gemm (marlin.cu:894).
  rank0/rank2/rank3 via eager dense wNa16 attn-proj (_custom_ops.py:1209,
  marlin_utils.py:717); rank1 via the compiled inductor graph (csfrz…py:1906) —
  same op, different call path. Hosts are LAUNCH-BLOCKED (queue full), not
  allocating: no malloc anywhere in the stacks.
- nvidia-smi-rank0.txt — GPU 96 % util, 18.4 W, 0 % memory utilization during the
  wedge (96 % on all four nodes). A computing GB10 draws far more; a spin-loop
  kernel (marlin global-reduce lock wait) reports busy with zero memory traffic
  at near-idle power. The DEVICE is spinning; the queue cannot drain.
- freem-*.txt — 4.4–4.9 GB avail at freeze on every node: the pre-registered
  "stall with multiple GB free" falsification of memory-as-sole-cause TRIGGERED.
- 0x51-*.txt — 1–2 events/node in the window (launch-burst pattern, banked).

## Scored conclusions
1. Memory-exhaustion-as-sole-cause: FALSIFIED (pre-registered rule, clean).
2. §16/§18 "allocation wait under exhausted UMA" re-read: the deeper native
   stacks show launch-queue blockage behind a spinning device kernel; the
   memory picture was a correlate, not the mechanism.
3. rank2-hardware: DEAD as sole cause — victim rank is rank1 here (rank2×3, fleet×1,
   rank1×1 across five freezes). Rank-agnostic, timing-dependent, systemic.
4. Unification: NO cold ≥60K-token prefill has ever completed on this fleet
   (60K/100K/100K wedges today on fresh boots; the "cold 200K >1800 s" finding
   is plausibly the same wedge). All successful large prefills were
   prefix-cache-warm. Suspect: marlin large-M chunk GEMMs (M = 8192 batch) —
   decode/small-M has hundreds of clean steps banked.
5. Next lever (attempt 2): max_num_batched_tokens 8192→2048 (different marlin
   tile/split-k config class, 4× smaller launch backlog, 4× more host syncs).
   VLLM_MARLIN_USE_ATOMIC_ADD rejected: should_use_atomic_add_reduce()
   hard-Falses at n≥2048 — cannot reach these GEMMs.

For NVIDIA/RFC: this is a reproducible (n=3, ~15 s onset) device-side wedge of
sm80-compiled marlin wNa16 kernels on GB10/sm_121 under sustained large-M
chunked-prefill load, with hosts pinned in cuLaunchKernel and the GPU spinning
at idle power. Independent of free UMA (falsified), node (rank-agnostic), and
KV/ceiling config (reproduced at 200K and 120K served).
