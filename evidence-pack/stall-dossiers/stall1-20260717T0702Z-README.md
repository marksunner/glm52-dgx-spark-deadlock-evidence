# Climb-stall capture 2026-07-17T07:02:42Z — frozen RAS MISMATCH (spread 1) during warmup stage 1

## What happened
Warmup-path climb stage 1 (4164-rep / ~100K-token cold prefill, max_tokens=1) fired
06:59:05Z into a healthy enforce cluster (§6 receipts 4/4 posted 06:58Z, boot 06:37Z).
Freeze onset ~06:59:10-15Z, seconds into the prefill. Pre-registered stall rule
(RAS op counts frozen across 3x90s samples) fired 07:02:41Z -> this bundle, climb stopped.

## Signature (all receipts in this dir)
- RAS main TP comm 0x3fa04f90fd367ce9: AllReduce launched counts FROZEN at
  [1581, 1581, 1580, 1581] — rank 2 ONE op short. Other comms equal. All comm
  health flags clean (no async_error, no aborts). ras-final.txt, GLM52_RAS_DUMP in rank0 log.
- Steps frozen EQUAL [32,32,32,32]; inflight [1,1,1,1]; GPUs 96% all ranks; 0x51: ZERO
  new events during the stall window (newest burst 06:45Z = boot weight-load churn, banked class).
- D3 consensus guard (observe): would_enforce from x3 polls 06:59:25Z, escalating
  continuously; GLM52_RAS_TRIGGER_SIG + full GLM52_RAS_DUMP written in-log (Task 1
  evidence-before-exit VALIDATED live).
- D4 fingerprint: no mismatch lines — expected; wedge is INSIDE one monolithic
  execute_model, no next step ever starts (matches task4-instrument-assessment regime 1).
- Drafter gate: ARMED enforce 4/4, zero DIVERGENCE/FORCED — correct; seq ~100K is far
  below its 199,997 jurisdiction. Gate unimplicated (doctrine 4: platform ledger).

## Localization (py-spy, all 4 ranks, this dir)
Compiled inductor graph cg7e7ntnzlqcidfrnhoosronpf2z6iyl57ipjwfqifoqn4xumshr.py
(per-layer attention->MoE block, straight-line code, NO branches):
- line 1310: torch.ops._C.marlin_gemm (attention output projection, 6144x4096)
- line 1321: torch.ops.vllm.all_reduce "tp:0"  <- AllReduce #1581
- line 1332: torch.ops.vllm.moe_forward_shared
Ranks 0/1/3: hosts at line 1332 (launched AR #1581, devices presumably spinning in it,
launch queues backed up into marlin_gemm inside moe_forward_shared).
Rank 2: host BLOCKED INSIDE the op call at line 1310 — native stack shows
MainThread spinning in sched_yield inside torch.ops._C.marlin_gemm. It never launched
AR #1581. Straight-line graph => NOT a consensus/branch divergence. Single-rank
host-side spin inside a quantized GEMM op.

## Memory context (fleet-wide, free -m captured in-stall)
ALL FOUR nodes at ~123-124 GB used / 124.6 GB total, ~1 GB free, 3-5 GB swap IN USE.
The 100K prefill runs the GB10 UMA pool at its ceiling fleet-wide. Hypothesis: the
sched_yield spin is an allocation/workspace wait under exhausted UMA (adjacent to the
banked 0x51 memdesc-leak class — spin instead of hard NV_ERR_NO_MEMORY). Corollary
hypothesis: cold-200K ">1800s" prefill time may be memory-thrash-bound (swap active),
not compute-bound.

## Jurisdiction & classification
Platform ledger (doctrine 4). Type-B-adjacent (single-node stall, rank2) but presenting
as RAS spread 1 — the "leading edge" pattern: peers launch a collective the stalled
rank never reaches. rank2 is now the common denominator in THREE distinct freeze
signatures (R2a flashinfer kernel; R2b zmq dispatch; this marlin_gemm spin), with all
hardware counters clean each time.

## Instrument scorecard
- Climb stall rule (RAS-counts, not step_rate_hz): fired correctly at 3 samples.
- D3 observe: detected in ~15 s, dumped evidence in-log. In enforce it would have
  recovered the cluster ~27 min before the 1800 s engine timeout.
- step_rate_hz: 0.0 throughout (blind inside monolithic prefill — as banked).

## Sequel
Engine timeout (1800 s) expected ~07:29:06Z -> EngineDeadError -> self-shutdown
(exit receipts appended below by post-mortem). Per pre-registered rule: climb STOPPED,
no soak, no retry without Mark. Warmup route to the ceiling is blocked by this NEW
sub-boundary failure mode until adjudicated.
